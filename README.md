# RSS-Feed data blog-aggregator
Implementing the server side specs of go

## Im using CORS method to secure API

## Begin with: 
1. do go mod init github.com/username/package_name
2. do git init (this initialises git in the current repo) 
3. create .gitignore files
4. Load the environment variables, pull env from file through joho
5. run go mod vendor

## Spin up server
1. create a NewRouter and connect it to a server, initialise a handler for server
2. Server should listen and serve
3. request the localhost through a GET request - the output should be 404 (when srver is running)
4. Add CORS specs to router
5. create a .json file to help in sending json response back
6. create a handler to handle request, as our response pattern is ready
7. Now hook up http-handler to a specific http-method and path (router2.Get("/healthz")) using a new Chi-router2
8. Mount this new router2 to the first router1, this mount function says hey im creating router2 to be a child router to router1
9. configure helper function to respond with error

Note: so once you create a helper-function for repsonse, you should also create a handler function to handle that error

## Creating DB Client

1. We're using postgresql here
2. install postgres for windows using its offcial EDB site, install pgadmin - a GUI-client to interaction with DB, cmd tool and one system something along with it
3. create a new server called localhost, and create a database rssagg
4. we are installing sqlc - to handle queries, goose to handle migration to streamline and ease the process of accessing DB 

I faced a lot of problem when installing sqlc - coz of cgo problem, a simple fix was to just turn off the flag... but to know this i spent around 5-6hrs for a fix and got it later from claude...

## Creating Migration
Note: Goose starts working with comments and those comments also have syntactical meaning
1. create a yaml file having the envi specific details to run sqlc. Then create table, add down migration and run migration by connecting DB to CLI by adding DB_URL
2. to run migration copy dbURL from .env and cd to sql/schema and run:  goose postgres copiedURL up (for down migration use down instead of up)
3. check whether the migration is applied or not using pgadmin

## Create operation (POST)
1. create a sql query now -- add sqlc.yaml and then create directory called queries and then add query, by creating a function called CreateUser, which returns 1 row and takes 4 arguments ($ states that its an argument and it'll passed from the application code)
2. now switch back to home directory of repo and run sqlc generate - this generates go code for our sql
Note: never write anything in internal/databases folder.. its entirely generated by sqlc

### Connect db to go code 
3. Now connect db to go in main by accessing .env var and opening it with sql.Open()
4. for go to connect to postgre specific feature we need to "get" github.com/lib/pq alternatively we can also use: github.com/jackc/pgx/v5/stdlib 
5. now configure the handler_user within this, create a user
6. we are thinking of adding an apiKey for authentication, so we're using mig2 to alter the exisiting table by adding a new column which is not null and unique, but as we already have some values in DB we are specifying a default value which will be randomly generated sequence of bytes and hash it to fit to VARCHAR(64) using sha256 and encode it to hex
7. now make that changes in insert function too (this function is present in users.sql file)

Note: important thing about this file is that... the comment that we write as the first line is: name: Name of the function :number of arguments that returns it

Note: we have defined our own db model in models.go coz the model that is returned by sqlc in response has some styling issues so we designed our model models.go 

## Read operation (GET)
1. Write the query to get users based on apikey and run sqlc
2. Here we are allowing only the authenticated individuals having the API-key to access the record, so we will search for API-key in the authentication header, through GetAPIKey function in auth.go
3. create a user, the response returns API-Key use that in the next go request
Note: I made a mistake here, to render API_key just modeling the render models.go'd be enough but i thought of changing it in handler_user which was not required...
4. We need to send apikey in header as authentication and not in authentication tab LOL


## Creating feeds for the RSS server
1. create schema for feeds (this is a 1:n type of relationship, hence adding foreign key)
Note: while running a migration always note to run it in sql/schema with goose postgres url up/down, but for running a sql query, then just go to home directory and run sqlc generate

### Creating feed (POST)
1. create a new feed by adding a query in feeds.sql 
2. to write handler_query, we need to repeat the same line of auth code for api_key auth, so we're creating a middleware auth that handles this code of api_key usage in handler function 
Note: interal/auth/auth.go only contains info regarding how to strip header and give back a valid API-key
3. Follow same steps as we did in create user, and get user to create feed and get feed
Note: everytime you changed anything in the migration table, then need to run down migration first and then up the migration, if you run the up migration again, it wont show any changes
4. from create feed, we are trying to create the feed for a user (using api key of the user) by specifying the name of the feed and url of the feed

Note: the RSS feed is a structured xml doc that describes what each post on the blog says, may be either a link/ short-description

### Get all the feeds (GET)
1. this is not an authenticated endpoint, so the reponse is about all the rss feeds present. 
2. Add query, and add method

## Follow specific feeds (POST)
1. create a schema to follow specific feeds (this is a n:n relationship model, meaning a user can follow n feeds and a same feed can be followed by m users) storing the relationship between a user and all the feeds that they follow 
2. To follow a feed we need to create a record of a user following the feed - so an authenticated endpoint

So steps:
a. create table first and do migration
b. write the respective sql query and generate sqlc
c. create handler to handle the request
d. create end-point to access the request

Note: Mistakes done till now - the designing of API should be consisent, if you're using snake-case continue with snake-case, i used camelCase instead of snake case

## Unfollow a feed
1. This is our first query that is not retuning anything so in query we just specify it to execute
2. we are using id and user_id both in order to delete a feed coz, if by somehow a user gets id of another's user's feed he could delete it, without the other user's consent, so to avoid it we are using both user_id and id

## RSS Feed fetching
1. this part goes out and fetches new posts, by going out periodically. 
e.g. if there is a feed called myBlog this part goes out and see if there are any new blog posts, if so it fetches and displays
2. so we need a new column called last_fecthed_at in feeds table 
I made a hilarious mistake here, i named the coulmn last_updated_at and added it, but it was already present, the migration was successful... but later when i tried to run the query through sqlc generate it throwed error column not found... later i realised it that it was coz of the name that i've given to the column LOL
3. now, write the query to fetch the feeds based on priority : 
a. which were not fetched previously
b. those feeds which were fetched at earliest i.e. the farthest in the past (based on timestamp)
4. a second query to mark the feed as fetched -to say hey we've already fetched this feed
5. Create a function that takes RSS url and parse (return) it as actual body - in rss.go
the return item is of type RSS, and each RSS feed also has list of items and each item corresponds to new blog posts

Note: whenever we're trying to define a struct to unmarshall a json/xml then please use its correpsonding xml/json tag next to each field
6. Writing the scraper file - this function is running in background as our server
Note: In software terms a scraper is a software that fetches updates periodically.
This function takes concurrency and timeBetweenRequests as arguments, so the request is sent periodically to fetch n number of concurrent feeds
7. as we need to fetch the concurrent feeds returned individually at the same time we need have a sync mechanism, so we use to sync.waitgroup to wait until all the individual feeds are fetched and retuned back by all the go-routines

8. LOL it was fantastic to know its working!!!
yeah as soon as i start running the server the blog posts are being successfully bought in

## Saving blog posts into database
1. creating a new table in DB called posts, add a new query to create post
2. then add this inside scrapper, run it
Hurray!! you successfully added all the feeds to DB, but there exists a problem now..
we've added unique constraint, so as we run this again, we'll get a lot of errors coz unique constraint failed... but this is expected behaviour, so lets not log it

## LAST FEATURE...
user to be able to get a list of all the newst posts from feeds that they're following
1. This involves join query
2. this is an authenticated end point



# Overall:
we are mirrored the functioning of RSS feed server by configuring API's
1. For user - Create and Get (authenticated method)
POST  - /users with name in body.  Please copy the api_key in the response
GET  - /users, pass Authentication header with value ApiKey "value that you've copied" (note: wihtout double quotes LOL) then in response you'll get that user

2. For feed (with user_id as foreign key) - Create (authenticated method) and Get all feeds
POST  - /feeds with name and url as the body params, and pass ApiKey as auth header
GET - /feeds , no need to pass any auth, just get is sufficient

3. For feed_follow - Follow a feed (authenticated), List - all the feeds that a user follows (so authenticated) and unfollow
POST - /feed_follows , pass Auth header with ApiKey and in body pass "feed_id" to create a feed
GET - /feed_follows, pass Auth header 
DELETE - /feed_follows/{feedFollowID} - pass Auth header and nothing is required in body

## WayForward
1. Improve logging mechanism
2. Add a frontend/CLI to interact with API
3. this was just happy coding... so dont mind to go deep and rouge XD 